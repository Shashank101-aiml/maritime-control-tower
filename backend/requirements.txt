# Pinned to exact versions so builds are reproducible. Previously every
# entry was `>=`, which meant a fresh install could silently pull a
# breaking release.

# --- Web framework ---
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3
python-multipart==0.0.32

# --- Auth & rate limiting ---
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
slowapi==0.1.10

# --- Database ---
SQLAlchemy==2.0.52
alembic==1.19.1
# Postgres driver, used when DATABASE_URL points at Postgres
# (docker compose). SQLite needs no driver.
psycopg[binary]==3.3.5

# --- HTTP / streaming clients ---
requests==2.34.2
websockets==17.1

# --- Data & modelling ---
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0
lightgbm==4.7.0
xgboost==3.4.1
joblib==1.5.3
pyarrow==25.0.1
openpyxl==3.1.5
matplotlib==3.11.1

# --- Testing ---
pytest==9.1.1
httpx==0.28.1

# --- Optional -----------------------------------------------------------
# Neither is installed and neither is reachable from the running app:
# app/vectorstore/* is not imported by anything outside itself, and the
# openai imports in explanation_agent.py / embeddings.py are guarded by
# try/except. Install these only when wiring up the retrieval-backed
# explanation layer (roadmap slice 07).
#   chromadb==0.5.*
#   openai==1.*
